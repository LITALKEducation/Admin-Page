.wrangler/
frontend/node_modules/
frontend/dist/

# Per-agent adapter symlinks generated by `npx skills add` — they just point
# back at the real skills in .agents/skills/, which is what's tracked.
.claude/skills/
